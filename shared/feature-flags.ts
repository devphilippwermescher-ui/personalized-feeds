/**
 * Content Analytics is temporarily disabled while its collection model is
 * redesigned. Keep this shared so neither the extension nor dashboard can
 * accidentally enable only one half of the feature.
 */
export const CONTENT_ANALYTICS_ENABLED = false;
