/**
 * Compatibility facade. The calendar itself now lives in the shared dashboard
 * components directory because Content Analytics is its second real consumer.
 */
export { DateRangeCalendar as CustomDatePicker } from '../../../components/DateRangeCalendar';
